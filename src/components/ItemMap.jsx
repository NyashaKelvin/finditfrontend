const ItemMap = ({ className = "h-[400px]" }) => {
  return (
    <div className={`${className} glass-card flex items-center justify-center text-muted-foreground text-sm`}>
      Map view has been removed.
    </div>
  );
};
export default ItemMap;
